notify:
  webhooks:
    - url: https://ph.polsaker.com/harbormaster/hook/circleci/
machine:
  python:
    version: 3.4.2
  services:
     - redis
     - mysql
  environment:
    DB_USER: ubuntu
    DB_PASSWD: ''
    DB_NAME: 'circle_test'
